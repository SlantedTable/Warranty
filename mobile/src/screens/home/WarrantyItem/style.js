const React = require('react-native')

const { StyleSheet } = React

export default StyleSheet.create({
  titleStyle: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 30,
  },
  warrantyItem: {
    alignSelf: 'stretch',
    height: 100,
  },
})
